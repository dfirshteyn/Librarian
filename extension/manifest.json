{
  "manifest_version": 3,
  "name": "Librarian Capture",
  "version": "0.1.0",
  "description": "Thin capture source for the Librarian memory daemon. Grabs visible chat text and a manual selection, sends it to ws://localhost:4001 — does zero processing itself.",
  "permissions": ["activeTab", "scripting"],
  "host_permissions": ["<all_urls>"],
  "action": {
    "default_popup": "popup.html"
  }
}
